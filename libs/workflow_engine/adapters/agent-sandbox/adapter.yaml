name: agent-sandbox
version: 1.0.0
description: Agent Sandbox controller for Kubernetes agent runtime environments
phase: runtime
requires:
  - gitops-platform
  - kubernetes-api
provides:
  - agent-sandbox
