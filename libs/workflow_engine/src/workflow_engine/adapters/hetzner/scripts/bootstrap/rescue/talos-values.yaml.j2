# {{ env }} Environment Talos Values
# Managed in tenant repository
# Auto-generated by ZTC bootstrap

controlplane:
  name: {{ controlplane_name }}
  ip: "{{ controlplane_ip }}"
  rescue_password: "{{ rescue_password }}"

workers: []

cluster:
  name: {{ cluster_name }}
  podSubnet: "10.244.0.0/16"
  serviceSubnet: "10.96.0.0/12"
  dnsDomain: cluster.local

talos:
  version: {{ talos_version }}

kubernetes:
  version: v1.34.2
